```<Page x:Class="JPMCGenAI_v1._0.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="#FAF9F6"
      Title="LoginPage">

    <Page.Resources>
        <!-- Rounded TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="#D4AF37"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="20" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded PasswordBox Style -->
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="#D4AF37"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="20" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded Button Style -->
        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <Border Grid.Column="0" Background="#F5F3EF">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="JPMC-AI" Foreground="#D4AF37" FontSize="48" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Automated Test Execution Platform" Foreground="#8C8575" FontSize="16" Margin="0,0,0,30"/>
                <TextBlock Text="Powered by AI-driven test automation" Foreground="#A8A190" FontSize="13"/>
            </StackPanel>
        </Border>

        <!-- Right Panel -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="340">
            <TextBlock Text="Welcome Back" Foreground="#333" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Sign in to your account" Foreground="#A8A190" FontSize="13" Margin="0,0,0,30"/>

            <TextBlock Text="Username" Foreground="#555" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBox x:Name="UsernameTextBox"/>

            <TextBlock Text="Password" Foreground="#555" FontSize="12" FontWeight="SemiBold" Margin="15,0,0,8"/>
            <PasswordBox x:Name="PasswordBox"/>

            <Button Content="Sign In" Click="LoginButton_Click" Margin="0,20,0,10" 
                    Foreground="White" Background="#D4AF37"/>

            <Grid Margin="0,15,0,15">
                <Border Background="#E5E3DD" Height="1"/>
                <TextBlock Text="or" Foreground="#A8A190" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FAF9F6" Padding="8,0"/>
            </Grid>

            <Button Content="Create New Account" Click="RegisterButton_Click" 
                    Background="Transparent" BorderBrush="#D4AF37" Foreground="#D4AF37" BorderThickness="1"/>

            <TextBlock Text="© 2025 JPMC AI Platform" Foreground="#B0A78F" FontSize="11" TextAlignment="Center" Margin="0,30,0,0"/>
        </StackPanel>
    </Grid>
</Page>```
