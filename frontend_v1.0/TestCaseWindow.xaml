<Window x:Class="JPMCGenAI_v1._0.TestCaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Case Details" Height="600" Width="800"
        Background="Black"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="White" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="RowBackground" Value="Transparent" />
                <Setter Property="AlternatingRowBackground" Value="#222222" />
                <Setter Property="AutoGenerateColumns" Value="False" />
                <Setter Property="CanUserAddRows" Value="True" />
                <Setter Property="CanUserDeleteRows" Value="True" />
                <Setter Property="GridLinesVisibility" Value="All" />
            </Style>
            <Style TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="White" />
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#222222" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="BorderBrush" Value="White" />
                <Setter Property="BorderThickness" Value="0,0,1,1" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Test Case Details" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />
            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                <TextBlock Text="Test Case ID" Foreground="White" FontSize="14" Margin="0,0,0,5" />
                <TextBox x:Name="TestCaseIdTextBox" IsReadOnly="True" />
                <TextBlock Text="Description" Foreground="White" FontSize="14" Margin="0,10,0,5" />
                <TextBox x:Name="DescriptionTextBox" AcceptsReturn="True" Height="60" />
                <TextBlock Text="Prerequisite" Foreground="White" FontSize="14" Margin="0,10,0,5" />
                <TextBox x:Name="PrereqTextBox" />
                <TextBlock Text="Steps" Foreground="White" FontSize="14" Margin="0,10,0,5" />
                <DataGrid x:Name="StepsDataGrid" Height="200" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Step" Binding="{Binding StepText}" Width="300" />
                        <DataGridTextColumn Header="Argument" Binding="{Binding Argument}" Width="300" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Save" Click="SaveButton_Click" Margin="0,0,10,0" />
                <Button Content="Cancel" Click="CancelButton_Click" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
