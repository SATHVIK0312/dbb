<Window x:Class="JPMCGenAI_v1._0.ExecutionLogViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Execution Logs"
        Height="620"
        Width="960"
        Background="#FAF9F6"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- JPMC Flux Gold Theme -->
        <SolidColorBrush x:Key="Gold" Color="#D4AF37"/>
        <SolidColorBrush x:Key="LightGold" Color="#FFF8F0"/>
        <SolidColorBrush x:Key="DarkText" Color="#333333"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E2E1DC"/>
    </Window.Resources>

    <!-- Main Elegant Card -->
    <Border Margin="24" 
            Background="White" 
            CornerRadius="24" 
            BorderBrush="#E2E1DC" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Opacity="0.15" ShadowDepth="12" Color="#000000"/>
        </Border.Effect>

        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Execution Logs"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="#D4AF37"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Detailed step-by-step execution output"
                           FontSize="15"
                           Foreground="#8C8575"
                           HorizontalAlignment="Center"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <!-- Log Viewer -->
            <Border Grid.Row="1"
                    Background="#0F1419"
                    CornerRadius="18"
                    BorderBrush="#333333"
                    BorderThickness="1"
                    Padding="20">

                <TextBox x:Name="LogsTextBox"
                         Background="Transparent"
                         Foreground="#00FF9D"
                         FontFamily="Consolas"
                         FontSize="13.5"
                         FontWeight="Medium"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         IsReadOnly="True"
                         BorderThickness="0"
                         Padding="12"
                         CaretBrush="#00FF9D"
                         SelectionBrush="#D4AF37">

                    <!-- Optional: Add subtle grid lines or line numbers later if desired -->
                    <TextBox.Resources>
                        <Style TargetType="ScrollBar">
                            <Setter Property="Background" Value="#1E1E1E"/>
                            <Setter Property="BorderBrush" Value="#333333"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Border>
        </Grid>
    </Border>
</Window>
